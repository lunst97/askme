<% question ||= nil %>
<% action_icon = question.answer.blank? ? 'reply' : 'pencil' %>

<div class="question">
  <div class="question-content">
    <div class="question-text text-lead">
      <%= question.text %>
    </div>

    <% if question.answer.present? %>
      <div class="question-answer text-lead">
        <%= question.answer if question.answer.present? %>
      </div>
    <% end %>

    <div class="question-date">
        <%= 'Спросил | ' %>
      <% if question.author_id.nil? %>
        <%= 'Аноним' %>
      <% else %>
        <%= link_to "@#{search_question_author(question.author_id)}", user_path(question.author_id) %>
      <% end %>
      <%= "(#{l(question.created_at, format: :short)})" %>
    </div>
  </div>

  <% if question.user == current_user %>
    <div class="question-actions">
      <%= link_to fa_icon(action_icon), edit_question_path(question) %>
      <%= link_to fa_icon('trash-o'), question_path(question), method: :delete, data: {confirm: 'Точно удалить вопрос?'}%>
    </div>
  <% end %>
</div>
